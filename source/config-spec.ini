[hybri_func]
hybridization_func_file=string
zero_rounding=float(min=0)
gap_pos=float(min=0)
gap_neg=float(min=0)

[discretization]
num_sites_star=integer
twisting_parameters=float_list
num_x=integer
energy_scaling=option('log','adaptive','sclog','linear','adaptive_linear')
Lambda=float(min=1)

[tridiagonalization]
num_sites_wilson=integer
vector_normalization_method=option('qr','sqrtm','mpqr')

[global]
data_folder=string
data_token=string
